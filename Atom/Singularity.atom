Bootstrap:  docker
From: ubuntu:18.04

%runscript
  /usr/bin/atom $* 

%setup

%files

%environment


%post
  apt-get update && apt-get install -y git gconf2 gconf-service libgtk2.0-0 libnotify4 libxtst6 \
          libnss3 python gvfs-bin xdg-utils libxss1  liboss4-salsa-asound2 wget apt

  wget -O atom-amd64.deb https://atom.io/download/deb
  dpkg -i atom-amd64.deb
  #apt install -yf /tmp/atom-amd64.deb
  apt-get install -yf && rm -rf /var/lib/apt/lists/*
